# dataSource
jdbc.driver = ${jdbc.driver}
jdbc.url = ${jdbc.url}
jdbc.user = ${jdbc.username}
jdbc.password = ${jdbc.password}

# httpclient
httpclient.request.url = ${httpclient.request.url}

# redis
redis.maxTotal=100
redis.node.host=127.0.0.1
redis.node.port=6379